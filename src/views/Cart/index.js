import React from 'react';
import PropTypes from 'prop-types';
import {
  Grid,
  Typography,
  Card,
  CardHeader,
  CardContent,
  CardMedia,
} from '@material-ui/core';

const Cart = props => {
  return (
    <Grid>
        <Typography>CART SECTION</Typography>
    </Grid>
  );
};

Cart.propTypes = {};

export default Cart;
